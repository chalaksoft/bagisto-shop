# راهنمای نصب

نصب کامل حدود ۱۰ دقیقه طول می‌کشد. دو راه دارد؛ اولی را انتخاب کنید که به شما
می‌خورد.

- [هاست اشتراکی](#هاست-اشتراکی-سی‌پنل--دایرکت‌ادمین) — بدون SSH، بدون composer
- [سرور مجازی](#سرور-مجازی-vps) — با SSH

---

## قبل از شروع

روی هاست یا سرورتان این‌ها باید باشد:

| مورد | حداقل |
| --- | --- |
| PHP | ۸.۲ |
| MySQL | ۵.۷ (یا MariaDB ۱۰.۳) |
| افزونه‌های PHP | `zip` · `gd` · `intl` · `mbstring` · `curl` · `openssl` · `pdo_mysql` · `fileinfo` · `bcmath` |

اگر مطمئن نیستید، از پشتیبانی هاست بپرسید «PHP نسخهٔ ۸.۲ و افزونهٔ zip فعال
است؟» — همین دو تا معمولاً مشکل‌سازند.

**Node.js لازم نیست.** فایل‌های آمادهٔ ظاهر سایت داخل خود بسته هستند.

---

## هاست اشتراکی (سی‌پنل / دایرکت‌ادمین)

### ۱. دیتابیس بسازید

در پنل هاست → MySQL Databases:

- یک دیتابیس بسازید (مثلاً `myshop`)
- یک کاربر بسازید و رمزش را جایی یادداشت کنید
- کاربر را با دسترسی **All Privileges** به دیتابیس وصل کنید

### ۲. فایل‌ها را آپلود کنید

فایل `bagisto-shop.zip` را از [صفحهٔ Releases](https://github.com/chalaksoft/bagisto-shop/releases)
دانلود کنید. این فایل همهٔ کتابخانه‌ها را در خودش دارد، پس composer لازم نیست.

در File Manager هاست، آن را در **پوشهٔ بالای `public_html`** آپلود و باز کنید —
نه داخل `public_html`. یعنی:

```
/home/username/
├── bagisto/          ← بسته اینجا باز شود
└── public_html/      ← این پوشه پاک و دوباره ساخته می‌شود (مرحلهٔ ۴)
```

> **چرا بالای `public_html`؟** اگر همه‌چیز داخل پوشهٔ عمومی باشد، فایل `.env`
> شما — که رمز دیتابیس در آن است — از اینترنت قابل دانلود می‌شود. این تنها
> اشتباهی است که واقعاً خطرناک است.

اگر هاستتان اجازهٔ خروج از `public_html` را نمی‌دهد، بخش
[هاست‌های محدود](#هاست‌هایی-که-اجازهٔ-خروج-از-public_html-را-نمی‌دهند) را ببینید.

### ۳. فایل تنظیمات را بسازید

در پوشهٔ `bagisto` فایل `.env.example` را به `.env` تغییر نام دهید و این خط‌ها
را ویرایش کنید:

```env
APP_URL=https://yourdomain.com
APP_LOCALE=fa

DB_DATABASE=نام_دیتابیس
DB_USERNAME=کاربر_دیتابیس
DB_PASSWORD=رمز_دیتابیس
```

### ۴. `public_html` را به فروشگاه وصل کنید

محتویات پوشهٔ `bagisto/public` را داخل `public_html` کپی کنید. بعد فایل
`public_html/index.php` را باز کنید و دو مسیر را اصلاح کنید:

```php
require __DIR__.'/../bagisto/vendor/autoload.php';

$app = require_once __DIR__.'/../bagisto/bootstrap/app.php';
```

(فقط `/../` را به `/../bagisto/` تبدیل کنید.)

### ۵. نصب را اجرا کنید

آدرس زیر را در مرورگر باز کنید:

```
https://yourdomain.com/installer
```

نصب‌کنندهٔ بجیستو مرحله‌به‌مرحله جلو می‌رود: بررسی سرور، اطلاعات دیتابیس، ساخت
مدیر. **زبان را فارسی و ارز را تومان انتخاب کنید.**

### ۶. تمام

- فروشگاه: `https://yourdomain.com`
- پنل مدیریت: `https://yourdomain.com/admin`

---

## سرور مجازی (VPS)

```bash
git clone https://github.com/chalaksoft/bagisto-shop.git
cd bagisto-shop

composer install --no-dev --optimize-autoloader

cp .env.example .env
php artisan key:generate
```

`.env` را ویرایش کنید (`APP_URL` و اطلاعات دیتابیس)، بعد:

```bash
php artisan bagisto:install
php artisan migrate
php artisan storage:link
```

دسترسی‌ها:

```bash
chown -R www-data:www-data storage bootstrap/cache public
chmod -R 775 storage bootstrap/cache
```

در تنظیمات وب‌سرور، **document root** را روی پوشهٔ `public` بگذارید — نه ریشهٔ
پروژه.

> ⚠️ اگر artisan را با `root` اجرا کنید، پوشه‌هایی که داخل `storage` ساخته
> می‌شوند مال root می‌مانند و کاربر وب نمی‌تواند در آن‌ها بنویسد. نتیجه‌اش
> خطای ۵۰۰ روی صفحه‌های تصادفی است که شبیه باگ برنامه به نظر می‌رسد و نیست.
> دستورها را با کاربر وب بزنید:
>
> ```bash
> sudo -u www-data php artisan optimize:clear
> ```

---

## نصب ماژول از پنل

اینجاست که این نسخه با بجیستوی معمولی فرق دارد: ماژول‌ها را از داخل پنل نصب
می‌کنید، بدون SSH و بدون composer.

### ۱. توکن را در `.env` بگذارید

توکن را موقع خرید ماژول از ما می‌گیرید:

```env
MARKETPLACE_URL=https://bagisto-shop.ir
MARKETPLACE_TOKEN=mkt_...
MARKETPLACE_REQUIRE_SIGNATURE=true
```

توکن **به دامنهٔ شما بسته است**. اگر آدرس سایت را عوض کردید، به ما بگویید تا در
مخزن به‌روز شود، وگرنه پیام «این دامنه در لایسنس ثبت نشده است» می‌گیرید.

### ۲. کلید عمومی مخزن

فایل `repository.pub` را که همراه توکن می‌فرستیم، اینجا بگذارید:

```
Modules/Marketplace/resources/keys/repository.pub
```

بدون این فایل هیچ ماژولی نصب نمی‌شود. دلیلش امنیت است: نصب ماژول یعنی «کد PHP
تازه روی سرور شما اجرا شود»، و امضای دیجیتال تنها چیزی است که ثابت می‌کند بسته
واقعاً از ما آمده و وسط راه دستکاری نشده.

### ۳. از پنل نصب کنید

پنل مدیریت → **ماژول‌ها** → **مخزن** → دکمهٔ **نصب**.

نصب مرحله‌به‌مرحله جلو می‌رود و نوار پیشرفت را می‌بینید. صفحه را نبندید.

> ماژول تازه‌نصب از **درخواست بعدی** فعال می‌شود. اگر بلافاصله اثری ندیدید،
> یک‌بار صفحه را دوباره باز کنید. این طبیعی است، خرابی نیست.

### به‌روزرسانی

در صفحهٔ «ماژول‌های نصب‌شده»، کنار نسخهٔ هر ماژول اگر نسخهٔ جدیدتری باشد
«↑ x.y.z موجود است» می‌بینید. روی VPS می‌توانید در cron هم بگذارید:

```bash
php artisan module:update
```

---

## وقتی چیزی کار نمی‌کند

### صفحهٔ سفید یا خطای ۵۰۰

اول لاگ را ببینید: `storage/logs/laravel.log` — آخرین خط‌هایش معمولاً دقیقاً
می‌گوید چه شده.

بعد دسترسی‌ها را چک کنید. `storage` و `bootstrap/cache` باید برای کاربر وب
قابل نوشتن باشند.

### «۴۰۴ Not Found» روی همهٔ صفحه‌ها جز اول

`mod_rewrite` خاموش است یا فایل `.htaccess` آپلود نشده. `.htaccess` فایل مخفی
است؛ در File Manager گزینهٔ «Show hidden files» را روشن کنید و مطمئن شوید کنار
`index.php` هست.

### تصویرها بالا نمی‌آیند

```bash
php artisan storage:link
```

اگر هاستتان symlink را نمی‌پذیرد، محتویات `storage/app/public` را دستی داخل
`public/storage` کپی کنید.

### «فایل بسته یک zip معتبر نیست» موقع نصب ماژول

افزونهٔ `zip` روی PHP فعال نیست. از پشتیبانی هاست بخواهید روشنش کند.

### تنظیمات را عوض کردم ولی اثری ندارد

```bash
php artisan optimize:clear
```

### بعد از نصب ماژول، آدرس‌هایش ۴۰۴ می‌دهد

اگر روی سرور از کش روت استفاده می‌کنید، یک‌بار بسازیدش:

```bash
php artisan route:cache
```

---

## پشتیبانی

- ایمیل: info@chalaksoft.com
- مشکلات فنی: [Issues](https://github.com/chalaksoft/bagisto-shop/issues)

---

## هاست‌هایی که اجازهٔ خروج از `public_html` را نمی‌دهند

اگر هیچ راهی نیست و مجبورید همه‌چیز را داخل `public_html` بگذارید، **حتماً**
این فایل `.htaccess` را در ریشهٔ پروژه بسازید تا فایل‌های حساس از اینترنت
قابل دانلود نباشند:

```apache
<FilesMatch "^\.env|composer\.(json|lock)$">
    Require all denied
</FilesMatch>

RedirectMatch 403 ^/(app|bootstrap|config|database|storage|vendor|Modules)/
```

این راه‌حل دوم است، نه اول. اگر می‌توانید هاست عوض کنید، عوض کنید.
